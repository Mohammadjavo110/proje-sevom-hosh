# proje-sevom-hosh
# ♟️ 6x6 Checkers AI Agent


##  معرفی پروژه
این پروژه پیاده‌سازی بازی دامه (Checkers) در ابعاد ۶×۶ است که در آن یک عامل هوشمند (AI Agent) با استفاده از الگوریتم‌های جستجو و یادگیری ماشین طراحی شده است. هدف اصلی، ساخت عاملی است که بتواند با استفاده از الگوریتم **Minimax** و هرس **Alpha-Beta**، بهترین حرکت ممکن را در برابر حریف (انسان یا عامل دیگر) انتخاب کند.

همچنین، یک مکانیزم **یادگیری تقویتی (Reinforcement Learning)** ساده پیاده‌سازی شده است که به عامل اجازه می‌دهد با هر بار بازی کردن، وزن‌های تابع ارزیابی خود را بهینه کرده و تجربه کسب کند.

***

## ویژگی‌های کلیدی
1.  **محیط بازی کامل:** پیاده‌سازی دقیق قوانین حرکت، تبدیل سرباز به شاه (King) و شناسایی پایان بازی.
    *   *نکته:* طبق تصمیم طراحی، قانون "خوردن اجباری" (Mandatory Jump) برای آزادی عمل بیشتر بازیکن غیرفعال شده است، اما مکانیزم "خوردن زنجیره‌ای" (Multi-jump) کاملاً فعال است.
2.  **الگوریتم‌های هوشمند:**
    *   پیاده‌سازی **Minimax** با عمق قابل تنظیم (پیش‌فرض: ۵).
    *   پیاده‌سازی **Alpha-Beta Pruning** برای بهینه‌سازی سرعت جستجو و کاهش گره‌های بررسی شده.
3.  **رابط گرافیکی (GUI):** استفاده از کتابخانه `tkinter` برای نمایش بصری، انیمیشن حرکت مهره‌ها و نمایش زنده آمار یادگیری.
4.  **یادگیری از تجربه (Bonus):** ذخیره‌سازی نتایج بازی‌ها در فایل `json` و اصلاح وزن‌های استراتژیک (مانند ارزش شاه یا کنترل مرکز) پس از هر بازی.

***

##  پیش‌نیازها و نحوه اجرا

### کتابخانه‌های مورد نیاز:
تمام کتابخانه‌ها در پکیج استاندارد پایتون موجود هستند:
*   `tkinter` (برای رابط گرافیکی)
*   `copy`
*   `random`
*   `json`
*   `os`

### نحوه اجرا:
1. فایل پروژه (`checkers_ai.py`) را دانلود کنید.
2. ترمینال یا CMD را در پوشه پروژه باز کنید.
3. دستور زیر را وارد کنید:
4. و یا کد را در ادیتور های برنامه نویسی ران کنید.
   ```bash
   python checkers_ai.py
   ```

***

##  تحلیل فنی ماژول‌ها

### ۱. کلاس `CheckersLogic` (محیط بازی)
این کلاس مسئول منطق بازی است. صفحه بازی به صورت یک ماتریس ۶×۶ مدل‌سازی شده است.
*   **تولید حرکات (`get_valid_moves`):** تمام حرکات مورب ساده و پرشی را محاسبه می‌کند. برای حرکات چندمرحله‌ای (Chain Jumps) از روش بازگشتی استفاده شده است.
*   **اعمال حرکت (`make_move`):** مهره را جابجا کرده، مهره‌های حریف را حذف می‌کند و وضعیت شاه شدن را بررسی می‌کند.

### ۲. کلاس `AI_Agent` (هوش مصنوعی)
مغز متفکر بازی که از الگوریتم‌های زیر استفاده می‌کند:
*   **Alpha-Beta Pruning:** با دو متغیر `alpha` و `beta` شاخه‌هایی از درخت جستجو که تاثیری در نتیجه نهایی ندارند را هرس می‌کند.
*   **تابع ارزیابی (`evaluate`):** به جای استفاده از مقادیر ثابت، از یک دیکشنری وزن‌ها (`self.weights`) استفاده می‌کند. این وزن‌ها شامل:
    *   `w_piece`: ارزش مهره عادی
    *   `w_king`: ارزش شاه (معمولاً ۱.۵ تا ۲ برابر مهره عادی)
    *   `w_center`: امتیاز کنترل خانه‌های مرکزی
    *   `w_edge`: امتیاز امنیت در کناره‌ها

### ۳. مکانیزم یادگیری (Learning Mechanism)
در پایان هر بازی، متد `learn_from_game` فراخوانی می‌شود:
*   **در صورت برد:** وزن ویژگی‌های موثر (مانند شاه و مرکز) مقدار کمی افزایش می‌یابد (Reinforcement).
*   **در صورت باخت:** وزن‌ها به صورت تصادفی کمی تغییر می‌کنند (Perturbation) تا عامل از مینیمم محلی خارج شود و استراتژی جدیدی را امتحان کند.
*   اطلاعات در فایل `ai_memory.json` ذخیره می‌شود تا در اجراهای بعدی استفاده شود.

***

##  نحوه کار با برنامه
پس از اجرای برنامه، منویی نمایش داده می‌شود:
1.  **Human vs AI:** شما (مهره سفید) در برابر هوش مصنوعی (مهره قرمز) بازی می‌کنید. برای حرکت، ابتدا روی مهره خود و سپس روی خانه مقصد کلیک کنید.
2.  **AI vs AI (Train):** هوش مصنوعی در برابر یک عامل تصادفی بازی می‌کند. این حالت برای آموزش سریع و مشاهده روند یادگیری طراحی شده است.

*در بالای صفحه، تعداد بازی‌های انجام شده، تعداد بردها و وزن‌های فعلی هوش مصنوعی به صورت زنده نمایش داده می‌شود.*

***

## 📝 منابع
*   مستندات درس هوش مصنوعی
*   قوانین استاندارد Checkers (دامه)
*   مستندات کتابخانه Tkinter Python

[1](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/attachments/images/138180981/e96f2156-51d5-4527-b7bb-e574a2fcb2dd/image.jpg)          
